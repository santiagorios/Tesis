$('.entries').append('<%= j render(@departments) %>');
<% if @departments.next_page %>
$('.pagination').replaceWith('<%= j will_paginate(@departments, :renderer => BootstrapPagination::Rails) %>');
<% else %>
$('.pagination').remove();
<% end %>