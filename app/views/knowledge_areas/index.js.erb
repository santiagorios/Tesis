$('.entries').append('<%= j render(@knowledge_areas) %>');
<% if @knowledge_areas.next_page %>
$('.pagination').replaceWith('<%= j will_paginate(@knowledge_areas, :renderer => BootstrapPagination::Rails) %>');
<% else %>
$('.pagination').remove();
<% end %>