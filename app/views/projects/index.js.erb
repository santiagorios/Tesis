$('.entries').append('<%= j render(@projects) %>');
<% if @projects.next_page %>
$('.pagination').replaceWith('<%= j will_paginate(@projects, :renderer => BootstrapPagination::Rails) %>');
<% else %>
$('.pagination').remove();
<% end %>