$('.entries').append('<%= j render(@research_groups) %>');
<% if @research_groups.next_page %>
$('.pagination').replaceWith('<%= j will_paginate(@research_groups, :renderer => BootstrapPagination::Rails) %>');
<% else %>
$('.pagination').remove();
<% end %>