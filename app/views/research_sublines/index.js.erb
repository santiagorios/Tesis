$('.entries').append('<%= j render(@research_sublines) %>');
<% if @research_sublines.next_page %>
$('.pagination').replaceWith('<%= j will_paginate(@research_sublines, :renderer => BootstrapPagination::Rails) %>');
<% else %>
$('.pagination').remove();
<% end %>